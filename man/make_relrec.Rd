% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers-supp.R
\name{make_relrec}
\alias{make_relrec}
\title{Assemble a RELREC dataset from one-to-one record links}
\usage{
make_relrec(links, study_id)
}
\arguments{
\item{links}{A data frame with columns \code{USUBJID}, \code{RDOMAIN_1}, \code{IDVAR_1},
\code{IDVARVAL_1}, \code{RDOMAIN_2}, \code{IDVAR_2}, \code{IDVARVAL_2} - one row per link.}

\item{study_id}{Study identifier, stamped into STUDYID and the RELID key.}
}
\value{
The RELREC data frame, ordered and labelled.
}
\description{
RELREC holds one row per linked record: every link in \code{links} becomes two
rows - one per domain - sharing a RELID. The links are record-level
(IDVAR / IDVARVAL populated), so RELTYPE stays null: per SDTMIG it
describes a dataset-level relationship, and populating it on
record-level links is what Pinnacle 21 flags.
}
