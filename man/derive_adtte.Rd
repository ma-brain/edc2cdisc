% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adam-adtte.R
\name{derive_adtte}
\alias{derive_adtte}
\title{Derive ADTTE, the time-to-event analysis dataset}
\usage{
derive_adtte(adsl, adae)
}
\arguments{
\item{adsl}{The ADSL dataset (see \code{\link[=derive_adsl]{derive_adsl()}})}

\item{adae}{The ADAE dataset (see \code{\link[=derive_adae]{derive_adae()}})}
}
\value{
The labelled ADTTE tibble.
}
\description{
Two parameters, no spec table - nothing here is study-specific. OS:
event at death (\code{ADSL} \code{DTHFL} = "Y", \code{ADT} = \code{DTHDT}), otherwise
censored at \code{EOSDT}. TTAE: event at the first treatment-emergent AE
(\code{ADAE} \code{TRTEMFL} = "Y", earliest \code{ASTDT}, ties broken by lowest
\code{ASEQ}), otherwise censored at \code{EOSDT}. \code{STARTDT} is \code{TRTSDT} and
\code{AVAL} = \code{ADT} - \code{STARTDT} through the shared day rule, so a same-day
event is day 1.
}
\examples{
ext <- file.path(tempdir(), "ex-adtte")
\dontshow{
suppressMessages(generate_rave_extract(out = ext))
}
built <- build_all(ext)
adtte <- derive_adtte(built$adam$ADSL, built$adam$ADAE)
head(adtte[, c("USUBJID", "PARAMCD", "AVAL", "EVNTDESC", "CNSDTDSC")])
}
