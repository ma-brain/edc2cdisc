% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trial-design.R
\name{map_ts}
\alias{map_ts}
\title{Map the Trial Summary domain}
\usage{
map_ts(spec)
}
\arguments{
\item{spec}{A \code{study_spec} (see \code{\link[=new_study_spec]{new_study_spec()}})}
}
\value{
The labelled SDTM TS tibble.
}
\description{
One record per trial summary parameter, straight from \code{spec$ts}. The
value lives in TSVAL and TSVALNF is the null flavor when no value can
be provided - exactly one of the two is filled per row (checked at
construction). NARMS and PLANSUB are cross-checked against \code{spec$arms}
and \code{spec$study$n} by \code{validate_sdtm()}, not recomputed here: the
output is the spec, and the validator catches a spec that disagrees
with itself.
}
\examples{
ts <- map_ts(spec_synth01)
ts[, c("TSPARMCD", "TSVAL")]
}
