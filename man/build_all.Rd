% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build-all.R
\name{build_all}
\alias{build_all}
\title{Run the full EDC -> SDTM -> ADaM pipeline}
\usage{
build_all(extract_dir, spec = spec_synth01, sdtm_dir = NULL, adam_dir = NULL)
}
\arguments{
\item{extract_dir}{Directory holding the clinical view extract (as
written by \code{\link[=generate_rave_extract]{generate_rave_extract()}})}

\item{spec}{A \code{study_spec}; defaults to the shipped \link{spec_synth01}}

\item{sdtm_dir}{Optional output directory for the SDTM domains. When
given, each domain is written as \verb{<domain>.rds} plus \verb{xpt/<domain>.xpt},
and a \code{define.xml} stub is written alongside.}

\item{adam_dir}{Optional output directory for the ADaM datasets, laid out
like \code{sdtm_dir}.}
}
\value{
A list with \code{sdtm} (named list of the 14 mapped domains) and
\code{adam} (ADSL, ADAE, ADVS, ADLB), invisibly.
}
\description{
Reads the clinical view extract from \code{extract_dir}, maps every SDTM
domain through the study spec, validates them, derives the ADaM analysis
datasets, validates those too, and optionally writes RDS + SAS v5 XPT
files plus a define.xml stub. Any validation ERROR stops the build before
anything is written.
}
\examples{
# regenerate the synthetic extract and run the whole pipeline
ext <- file.path(tempdir(), "extract")
\dontshow{
suppressMessages(generate_rave_extract(out = ext))
}
built <- build_all(ext)

# or, writing the deliverables as RDS + XPT + define.xml
out <- file.path(tempdir(), "deliverables")
build_all(ext, sdtm_dir = file.path(out, "sdtm"),
          adam_dir = file.path(out, "adam"))
}
