% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers-supp.R
\name{make_supp}
\alias{make_supp}
\title{Assemble a Supplemental Qualifiers (SUPP--) dataset}
\usage{
make_supp(parent, rdomain, idvar, qnams)
}
\arguments{
\item{parent}{Parent data frame. Must contain STUDYID, USUBJID, every
column named in \code{qnams$src} (defaulting to \code{qnams$qnam}), and - unless
\code{idvar} is NA - the column named by \code{idvar}.}

\item{rdomain}{Parent domain code, e.g. "DM" or "AE".}

\item{idvar}{Name of the variable that ties a qualifier to one parent
record, e.g. "AESEQ". NA for a one-record-per-subject parent such as DM,
where IDVAR / IDVARVAL are left blank and USUBJID alone is the link.}

\item{qnams}{A data frame of qualifiers, one row per QNAM, with columns
\code{qnam}, \code{qlabel}, optionally \code{src} (the column in \code{parent} holding the
value; defaults to \code{qnam}), \code{qorig} (default "CRF") and \code{qeval}
(default NA).}
}
\value{
The SUPP-- data frame, ordered and labelled.
}
\description{
Pivots one or more non-standard parent-domain columns into the long
RDOMAIN / IDVAR / IDVARVAL / QNAM / QVAL structure. A row is emitted only
where the value is present: QVAL is required, so a blank qualifier simply
produces no SUPP record (a "specify" field appears only for the subjects who
triggered it; a Y/N field appears for everyone it was asked of).
}
\examples{
parent <- data.frame(
  STUDYID = "3021", USUBJID = c("3021-101-001", "3021-101-002"),
  AESEQ = c(1, 2), AESI = c("Y", "N"), AENOTE = c("", "details")
)
make_supp(parent, "AE", "AESEQ",
          qnams = data.frame(qnam = c("AESI", "AENOTE"),
                             qlabel = c("Special Interest", "Note")))
}
