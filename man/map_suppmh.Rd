% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/map-supp.R
\name{map_suppmh}
\alias{map_suppmh}
\title{Map the Supplemental Qualifiers for MH}
\usage{
map_suppmh(mh, mh_built, spec)
}
\arguments{
\item{mh}{Raw MH clinical view}

\item{mh_built}{The mapped MH dataset (see \code{\link[=map_mh]{map_mh()}})}

\item{spec}{A \code{study_spec}}
}
\value{
The labelled SUPPMH tibble.
}
\description{
MH is a log form, so the raw "If Related, Specify" text hangs on one log
line: the raw fields are joined onto the built MH by MHSPID (the log-line
number \code{\link[=map_mh]{map_mh()}} carries as the sponsor identifier - the AESPID trick in
\code{\link[=map_suppae]{map_suppae()}}) to recover the derived MHSEQ. The output IDVAR is
"MHSEQ", pointing each SUPP row at its parent record's key variable just
like SUPPAE's IDVAR = "AESEQ"; MHSPID is only the join key. Only lines
where the investigator actually specified a detail produce a SUPP row.
}
\details{
As in \code{\link[=map_supppe]{map_supppe()}}, the transformed value is stored under the \code{src}
name, not \code{qnam}: \code{\link[=make_supp]{make_supp()}} reads QVAL from the parent column named
in \code{qnams$src}, and the qualifier's src (MHSPEC) differs from its qnam
(MHSPECD).
}
\examples{
ext <- file.path(tempdir(), "ex-map-suppmh")
\dontshow{
suppressMessages(generate_rave_extract(out = ext))
}
forms <- suppressMessages(read_rave_extract(dir = ext))
dm <- map_dm(forms$DM, forms$EX, forms$DS, spec_synth01)
mh <- map_mh(forms$MH, spec_synth01, subject_ref(dm))
suppmh <- map_suppmh(forms$MH, mh, spec_synth01)
suppmh[, c("USUBJID", "IDVARVAL", "QNAM", "QVAL")]
}
