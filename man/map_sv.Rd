% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/map-sv.R
\name{map_sv}
\alias{map_sv}
\title{Map the Subject Visits domain}
\usage{
map_sv(forms, spec, refs)
}
\arguments{
\item{forms}{A named list of raw clinical views, as returned by
\code{\link[=read_rave_extract]{read_rave_extract()}}}

\item{spec}{A \code{study_spec}}

\item{refs}{Subject reference dates from \code{\link[=subject_ref]{subject_ref()}}}
}
\value{
The labelled SDTM SV tibble.
}
\description{
Which forms contribute a visit is spec-driven: every form marked
\code{scheduled} in \code{spec$forms} (the event forms on real folders). Log forms
live on Folder = "LOG" and never contribute.
}
\examples{
ext <- file.path(tempdir(), "ex-map-sv")
\dontshow{
suppressMessages(generate_rave_extract(out = ext))
}
forms <- suppressMessages(read_rave_extract(dir = ext))
dm   <- map_dm(forms$DM, forms$EX, forms$DS, spec_synth01)
sv   <- map_sv(forms, spec_synth01, subject_ref(dm))
head(sv[, c("USUBJID", "VISITNUM", "VISIT", "SVSTDTC")])
}
