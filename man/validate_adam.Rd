% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate-adam.R
\name{validate_adam}
\alias{validate_adam}
\title{Validate the derived ADaM datasets}
\usage{
validate_adam(
  adsl,
  adae,
  advs,
  adlb,
  dm,
  ds,
  ae,
  vs,
  lb,
  suppae,
  param_spec = tribble(~PARAMCD, ~ANRLO, ~ANRHI, "SYSBP", 90, 140, "DIABP", 50, 90,
    "PULSE", 50, 120, "TEMP", 35, 37.5, "WEIGHT", NA, NA, "HEIGHT", NA, NA)
)
}
\arguments{
\item{adsl, adae, advs, adlb}{The mapped ADaM datasets}

\item{dm, ds, ae, vs, lb, suppae}{The SDTM source datasets the ADaM layer was
built from}

\item{param_spec}{The declared reference ranges for ADVS (PARAMCD, ANRLO,
ANRHI). Defaults to an independent copy of the SYNTH01 SAP stand-in -
deliberately a second declaration, so a silent change on either side
(here or in \code{\link[=derive_advs]{derive_advs()}}) trips the range-drift check.}
}
\value{
An issue tibble: domain, severity ("ERROR" / "WARN"), check,
detail. Empty when everything passes.
}
\description{
Recomputes the derivations from their inputs instead of trusting the
build: population-flag and treatment coherence on ADSL, the TRTEMFL
window and SUPP merge-back on ADAE, and for the BDS datasets (ADVS /
ADLB) the baseline anchors, BASE/CHG/PCHG arithmetic, ANRIND against the
row's own range, and coverage against the SDTM source records.
}
