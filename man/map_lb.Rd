% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/map-lb.R
\name{map_lb}
\alias{map_lb}
\title{Map the Laboratory Test Results domain}
\usage{
map_lb(lb, spec, refs)
}
\arguments{
\item{lb}{Raw LB clinical view}

\item{spec}{A \code{study_spec}}

\item{refs}{Subject reference dates from \code{\link[=subject_ref]{subject_ref()}}}
}
\value{
The labelled SDTM LB tibble.
}
\description{
The per-analyte pivot spec comes from \code{spec$tests} (domain "LB") and the
conventional-to-SI conversion factors from \code{spec$units}. Baseline
(LBBLFL) is the last non-missing standard result on or before first dose.
}
\examples{
ext <- file.path(tempdir(), "ex-map-lb")
\dontshow{
suppressMessages(generate_rave_extract(out = ext))
}
forms <- suppressMessages(read_rave_extract(dir = ext))
dm <- map_dm(forms$DM, forms$EX, forms$DS, spec_synth01)
lb <- map_lb(forms$LB, spec_synth01, subject_ref(dm))
head(lb[, c("USUBJID", "LBTESTCD", "LBORRES", "LBSTRESN", "LBNRIND")])
}
