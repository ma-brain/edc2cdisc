% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rave-io.R
\name{read_clinical_view}
\alias{read_clinical_view}
\title{Read one clinical view CSV}
\usage{
read_clinical_view(form_oid, dir, active_only = TRUE)
}
\arguments{
\item{form_oid}{Form OID, e.g. "VS"}

\item{dir}{Directory holding the extract}

\item{active_only}{Drop soft-deleted records (RecordActive == "0")}
}
\value{
The form as a tibble: all columns character on read, with the
header bookkeeping columns typed (flags integer, timestamps POSIXct)
and FORMOID attached.
}
\description{
Everything is read as character on purpose: EDC CSV exports quote all
values, mix numeric and text across CRF versions, and use partial dates
that no date parser should be allowed to guess at. Typing happens later,
explicitly, per variable.
}
\details{
The file must end with an \code{EOF} marker row: its absence means the stream
was truncated by an API timeout, which is a hard error, not a warning.
}
