% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine.R
\name{map_variables}
\alias{map_variables}
\title{Map one domain's variables through the spec table}
\usage{
map_variables(data, spec, rows, derivations = list())
}
\arguments{
\item{data}{The in-flight source frame: raw clinical view columns plus
whatever glue columns the mapper joined on before calling this.}

\item{spec}{A \code{study_spec}}

\item{rows}{The variables-table rows for one domain, in order (usually
\code{filter(spec$variables, domain == "XX")})}

\item{derivations}{Additional named derivation functions with signature
\verb{function(data, spec)}; they extend the shared registry.}
}
\value{
A tibble with exactly the rows' variables, in table order.
}
\description{
Applies the \code{transform} vocabulary row by row, in table order, so later
rows see columns created by earlier ones (DM's ACTARMCD mirrors the
derived ARMCD, DMDY uses the derived DMDTC, and so on).
}
\keyword{internal}
