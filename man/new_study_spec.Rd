% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec.R
\name{new_study_spec}
\alias{new_study_spec}
\title{Build and validate a study specification}
\usage{
new_study_spec(
  study,
  sites,
  arms,
  visits,
  codelists,
  supp,
  units,
  forms,
  tests,
  bds,
  variables,
  derivations = list()
)
}
\arguments{
\item{study, sites, arms, visits, codelists, supp, units, forms, tests, bds, variables}{Tibbles as described above.}

\item{derivations}{Optional named list of extra derivation functions
(signature \verb{function(data, spec, row)}). Stored on \code{spec$derivations}
and read by \code{\link[=map_variables]{map_variables()}} by default; an explicit
\code{map_variables(derivations = )} argument still overrides. Declared
here so the reference check can see them.}
}
\value{
A \code{study_spec} object (a validated named list).
}
\description{
A \code{study_spec} is a list of tibbles. Each table carries one kind of
study-specific mapping configuration:
}
\details{
\itemize{
\item \code{study}     - study constants: \code{STUDYID}, \code{PROJECT}, \code{seed}, \code{n},
\code{age_min} / \code{age_max} (the bounds \code{\link[=validate_adam]{validate_adam()}} applies to AGE)
\item \code{sites}     - site table: \code{SiteNumber}, \code{siteid}, \code{Site}, \code{SiteGroup},
\code{StudySiteId}, \code{COUNTRY}
\item \code{arms}      - planned arm decode -> \code{ARMCD} / \code{ARM} (screen failures are
not randomised and are handled by a derivation, not a row here)
\item \code{visits}    - folder OID -> \code{VISITNUM} / \code{VISIT} / \code{EPOCH} /
\code{TargetDays} (joined onto scheduled-visit domains)
\item \code{codelists} - long controlled-terminology table: \code{ct}, \code{rave_decode},
\code{cdisc_term}. One \code{ct} group per mapped variable; unmapped collected
values break the run loudly (see \code{\link[=check_ct]{check_ct()}}).
\item \code{supp}      - SUPP-- qualifier specs: \code{rdomain}, \code{idvar}, \code{qnam},
\code{qlabel}, \code{src} (raw column holding the value), \code{transform} (how to
derive QVAL from \code{src}), \code{qorig}, \code{qeval}
\item \code{units}     - analyte-aware conventional -> SI conversions for LB:
\code{testcd}, \code{conv_from}, \code{conv_to}, \code{conv_factor}
\item \code{forms}     - form typing: \code{form_oid}, \code{type} ("event" / "log"),
\code{scheduled} (does the form sit on a real scheduled folder and thus
contribute an SV visit?)
\item \code{tests}     - per-test pivot specs for the findings domains VS/LB:
\code{domain}, \code{field} (raw field OID), \code{testcd}, \code{test}, \code{cat}, \code{specimen}
\item \code{bds}       - ADaM BDS parameter configuration: \code{domain} (ADVS / ADLB),
\code{paramcd}, \code{paramn} (order), \code{anrlo}, \code{anrhi} (declared reference
ranges; NA where a parameter has no absolute range)
\item \code{variables} - the mapping table proper, one row per SDTM variable per
domain: \code{domain}, \code{variable}, \code{crf_field}, \code{transform}, \code{ref},
\code{value} (for constants), \code{aux} (auxiliary raw column, e.g. the time
companion of a --DTC), \code{default} (decode fallback). \code{transform}
names a member of the fixed vocabulary; \code{derivation} rows name a
registered derivation in \code{ref}.
}

The constructor validates references as well as shapes: every
\code{derivation} row's \code{ref} must resolve against the derivation registry
(the shared one plus \code{derivations}), every \code{decode} row's \code{ref} must
name a codelist the spec carries, every \code{supp$transform} must be one
\code{supp_transform()} knows, and every \code{variables$domain} must be a domain
the package can build. A spec that cannot be honoured fails here, not
halfway through a build.
}
