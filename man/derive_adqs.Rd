% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adam-adqs.R
\name{derive_adqs}
\alias{derive_adqs}
\title{Derive ADQS, the questionnaire analysis dataset (BDS)}
\usage{
derive_adqs(qs, adsl, spec = spec_synth01)
}
\arguments{
\item{qs}{The mapped SDTM QS dataset}

\item{adsl}{The ADSL dataset (see \code{\link[=derive_adsl]{derive_adsl()}})}

\item{spec}{A \code{study_spec}; the ADQS rows of \code{spec$bds} declare the item
parameters (codes, order, no ranges) and the \code{spec$totals} rows declare
the derived totals (code, name, order, reference range, \code{src_items})}
}
\value{
The labelled ADQS tibble.
}
\description{
The items are one record per subject per visit per QS item (AVAL =
QSSTRESN), exactly the ADVS/ADEG pattern on the QS domain. The derived
parameters come from \code{spec$totals}: for each row, the named \code{src_items}
are summed per (USUBJID, VISITNUM) and the total is emitted only where
\strong{all} source items are present with a value - the strict,
SAP-defensible default; a visit with a missing or unparseable item
scores nothing rather than a proration this house does not invent.
A not-done visit produces no items and therefore no total.
}
\examples{
ext <- file.path(tempdir(), "ex-adqs")
\dontshow{
suppressMessages(generate_rave_extract(out = ext))
}
forms <- suppressMessages(read_rave_extract(dir = ext))
built <- build_all(ext)
adqs <- derive_adqs(built$sdtm$QS, built$adam$ADSL, spec_synth01)
head(adqs[, c("USUBJID", "PARAMCD", "AVAL", "BASE", "CHG", "ANRIND")])
}
