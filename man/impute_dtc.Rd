% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers-dates.R
\name{impute_dtc}
\alias{impute_dtc}
\title{Impute a partial ISO 8601 date to a full Date, with an imputation flag}
\usage{
impute_dtc(dtc)
}
\arguments{
\item{dtc}{ISO 8601 character vector (full, reduced precision, or NA)}
}
\value{
A list with \code{date} (Date vector) and \code{flag} (\code{c("", "D", "M", NA)}).
}
\description{
The analysis-layer counterpart to \code{\link[=rave_dtc]{rave_dtc()}}: SDTM keeps reduced
precision, ADaM resolves it. A missing month and/or day is imputed to 01
(the "first" rule, also admiral's default) and the flag records what
happened: \code{""} complete, \code{"D"} day imputed, \code{"M"} month (and day) imputed.
}
\details{
Note the bias this rule carries: imputing a birth date to January 1st can
overstate AGE by up to 11 months. A study SAP often prefers mid-year here -
the point is to have one explicit, flagged rule rather than silence.
}
