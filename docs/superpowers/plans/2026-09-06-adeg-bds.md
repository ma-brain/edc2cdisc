# ADEG (BDS) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Unattended-overnight rules: never push, commit per task, keep the suite green (planned re-freeze windows called out), stop cleanly on BLOCKED. Branch: `adeg-bds`.

**Goal:** Add ADEG, the ECG analysis dataset (BDS), as the sixth ADaM dataset — `derive_adeg(eg, adsl, spec)` cloning `derive_advs()`, with declared reference ranges in `spec$bds` and one config-driven HIGH seed so ANRIND classifies.

**Architecture:** BDS stands on SDTM groundwork the EG mapper already did (EGSTRESN in standard units, EGBLFL the last pre-dose result). The ADVS template applies wholesale; the only new machinery is a seeded QT/QTCF override that makes ANRIND live.

**Tech Stack:** unchanged. **Spec:** `docs/superpowers/specs/2026-09-06-adeg-bds-design.md`.

## Global Constraints

- **Zero RNG**: the HIGH seed is a config-driven literal override.
- **Byte-freedom accounting**: EG.csv digest moves (value change on the seeded row — prove the diff is confined to that row's QT/QTCF values); **eg.rds re-frozen deliberately** (same confined diff); `adeg.rds` new; **all other digests and references byte-identical**.
- Suite baseline 777 pass / 0 warnings; ends ≥ 800 / 0 warnings.
- `validate_adam()` signature grows exactly once: `(adsl, adae, adcm, advs, adeg, adlb, dm, ds, ae, cm, vs, eg, lb, suppae, spec)`; all call sites updated in the same task.

## File Structure

- `R/generate-rave-extract.R` + `R/generator-configs.R` — `idx$eg_qt_high` + QT/QTCF override.
- `R/zzz-spec-synth01.R` / `-02.R` — `bds` rows for ADEG.
- `R/adam-adeg.R` (new) — `derive_adeg()`.
- `R/validate-adam.R` — the `adeg-*` check block (mirror of the ADVS block, `R/validate-adam.R:455-620`).
- `R/build-all.R`, `R/globals.R` (if needed), count tests, `tests/testthat/test-adeg.R` (new).
- `tests/reference/adam/adeg.rds` (new), `tests/reference/sdtm/eg.rds` (re-frozen).

---

### Task 1: The HIGH seed in the generator

**Files:** `R/generate-rave-extract.R`, `R/generator-configs.R`.

- [ ] Config: `idx$eg_qt_high = c(<idx>, <vpos>)` per study — pick alive indices (subject not SF/ET-shortened at that visit; SYNTH01 SF {5,17} ET {3,11,20}, SYNTH02 SF {6,14} ET {2,9}); probe the generated extract first; record in-config with comments.
- [ ] In the EG populate block, after the deterministic interval arithmetic: `if (idx == cfg$idx$eg_qt_high[1] && vpos == cfg$idx$eg_qt_high[2]) eg_fields$QT_RAW <- "520"; eg_fields$QTCF_RAW <- "537"` (inside the `eg_perf` branch — a not-done visit has no values to override).
- [ ] Verify: 13 files; the 12 non-EG digests unchanged; EG.csv's diff confined to the seeded row's QT_RAW/QTCF_RAW cells (show `git diff`-style or line-level evidence); digest-guard failures confined to EG.csv + file-count if asserted.
- [ ] Commit: `feat(generator): seeded HIGH QT/QTCF for ADEG ranges`.

### Task 2: spec$bds rows + re-freeze

**Files:** `R/zzz-spec-synth01.R`, `R/zzz-spec-synth02.R`, digest fixture, `tests/reference/sdtm/eg.rds`.

- [ ] `bds` tribble (both studies), after the ADLB rows:

```r
    # ADEG: the SAP stand-in ECG interval ranges (msec)
    "ADEG", "PR",   1,  120,  200,
    "ADEG", "QRS",  2,  60,   100,
    "ADEG", "QT",   3,  350,  450,
    "ADEG", "QTCF", 4,  350,  450,
    "ADEG", "RR",   5,  600,  1200
```

- [ ] Digest re-freeze (EG.csv mover) AND `eg.rds` re-freeze via `Rscript tests/update_reference.R` — the expected diff: EG.csv digest, `eg.rds` modified (seeded QT/QTCF values), NOTHING else. Verify `git diff --stat tests/reference` shows exactly eg.rds (binary) and inspect via `readRDS` that only the seeded subject-visit's QT/QTCF changed. Suite green again (FAIL 0 WARN 0).
- [ ] Commit: `feat(spec): ADEG parameter ranges; eg.rds re-frozen for the HIGH seed`.

### Task 3: `derive_adeg()`

**Files:** Create `R/adam-adeg.R`, `tests/testthat/test-adeg.R`; `R/globals.R` if needed.

**Interfaces:** `derive_adeg(eg, adsl, spec)` → columns exactly as ADVS minus VSPOS: STUDYID, USUBJID, PARAMCD, PARAM, PARAMN, AVAL, AVALU, ABLFL, BASE, CHG, PCHG, ANRIND, ANRLO, ANRHI, AVISIT, AVISITN, ADT, ADY.

- [ ] Failing tests first (`map-qs`-style fixture; probe values, don't invent): row count = non-NOT-DONE EG rows; PARAMCD set {PR,QRS,QT,QTCF,RR}; PARAM = "PR Interval (msec)" pattern; AVAL/ANRIND on the seeded HIGH subject-visit (QT/QTCF = HIGH, ANRLO/ANRHI from spec); ABLFL carried from EGBLFL; BASE/CHG/PCHG per the shared rules (baseline row: CHG/PCHG NA; screen failures: BASE/CHG NA); the reduced-precision EGDTC row has ADT and ADY; ADY equals `derive_dy_d(ADT, TRTSDT)` recomputed in-test from pinned dates; all-subjects key uniqueness.
- [ ] Implement: clone `derive_advs` (`R/adam-advs.R:64-116`) with EG substitutions — `filter(!EGSTAT %in% "NOT DONE")`, join keys `EGTESTCD`, `ABLFL = EGBLFL`, `ADT = dtc_date(EGDTC)`, `AVAL = EGSTRESN`, `AVALU = EGSTRESU`, drop the VSPOS tail. Roxygen in the ADVS voice; run its `@examples` smoke.
- [ ] `devtools::test(filter = "^adeg$")` green; full suite green (FAIL 0 WARN 0 — eg.rds already re-frozen); lint clean; document().
- [ ] Commit: `feat(adam): derive_adeg()`.

### Task 4: Validator mirror

**Files:** `R/validate-adam.R`, `tests/testthat/test-validators.R` (or `test-adeg.R` — follow where the ADCM validator meta-tests live: `test-validators.R`).

- [ ] Signature: `(adsl, adae, adcm, advs, adeg, adlb, dm, ds, ae, cm, vs, eg, lb, suppae, spec)`.
- [ ] New `adeg-*` block mirroring the ADVS block (`R/validate-adam.R:455-620`) check-for-check with EG substitutions: required-vars (param_spec-driven like ADVS), key-not-unique, coverage (vs EG non-STAT keys), orphan-record, ablfl-multi/missing/screenfail/not-from-eg, base-wrong/screenfail/missing, chg-wrong, range-spec-drift, anrind-wrong, ady-wrong, study-day-zero, param-not-in-spec.
- [ ] All existing call sites of `validate_adam` updated (build-all.R, tests) — TDD: signature change first (red on call sites), then wire.
- [ ] Meta-tests: corrupt an ADEG ADY → `adeg-ady-wrong`; ANRIND flip → `adeg-anrind-wrong`; drop a row → `adeg-coverage`; ABLFL manipulation → the matching ablfl check; clean build zero findings.
- [ ] Commit: `feat(validate): the ADEG contract`.

### Task 5: Wiring + counts + adeg.rds freeze

**Files:** `R/build-all.R`, count tests, `tests/reference/adam/adeg.rds`.

- [ ] `adeg <- derive_adeg(eg, adsl, spec)` in the ADaM block (after `advs`, per roxygen order or list order — pick after ADVS, before ADLB, and keep roxygen/list/test lists consistent); `adam` list + `ADEG = adeg`; roxygen "5 ADaM datasets" → 6 where hard-coded; document().
- [ ] Counts: adam rds set + `adeg.rds` (alphabetical: adcm, adeg, adlb...), adam xpt 5 → 6 (test-build-all.R, test-synth02.R), any adam setequal lists in test-trial-design.R.
- [ ] Freeze: `Rscript tests/update_reference.R` → "26 sdtm + 6 adam"; `git status --porcelain tests/reference` → exactly `?? tests/reference/adam/adeg.rds`, zero modified. **Anything else → STOP.**
- [ ] Full suite FAIL 0 WARN 0; lint clean; `R CMD build && R CMD check` Status: OK; clean artifacts.
- [ ] Commit: `feat(build): wire ADEG into build_all()` + `test: freeze the ADEG reference output` (two commits).

### Task 6: Docs

**Files:** `README.md`, `NEWS.md`, design doc if needed.

- [ ] README: "**Derives** the core ADaM datasets" line + quick-start comment gain ADEG (`# ADSL / ADAE / ADCM / ADEG / ADVS / ADLB, validated`). Verify no stale "5 ADaM"/"ADVS, ADLB"-only claims.
- [ ] NEWS: bullet under `0.4.5.9000` (new heading — 0.4.5 released):

```markdown
# edc2cdisc 0.4.5.9000

## Added

* ADEG, the ECG analysis dataset (BDS). `derive_adeg()` clones the ADVS
  pattern: AVAL from EGSTRESN, baseline/change derivations against the
  carried EGBLFL, and ANRIND against the five declared ECG interval ranges
  in `spec$bds` — exercised by a seeded HIGH QT/QTCF visit.
  `build_all()` returns 6 ADaM datasets.
```

- [ ] Verify: build_vignettes clean; full suite green; commit `docs: ADEG`.

---

## Self-review

Design-doc decisions → tasks: HIGH seed (1), spec$bds ranges (2), clone (3), validator mirror (6 checks↔4), 6 ADaM (5), docs (6). Byte accounting explicit per task. Known plan-risk: the ADVS-block mirror is the largest single edit — read `R/validate-adam.R:455-620` in full before writing.
